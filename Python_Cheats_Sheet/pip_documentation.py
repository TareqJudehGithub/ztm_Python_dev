"""
pip

 - pip (preferred installer program) comes installed with Python.
 - pip is the most famous and widely used package management system to install
   and manage software packages written in Python and found in Python Package 
   Index (PyPI).
""" 
# installation

# $ pip install --upgrade --pip
# download the latest get-pip.py script and run it.

"""
 pip list vs pip freeze
 - pip list shows ALL installed packages. 
 - pip freeze shows packages YOU installed via pip (or pipenv if using that 
   tool) command in a requirements format.
"""
# $ pip show package_name           displays current package_name version
# $ pip uninstall package_name      uninstalls a package